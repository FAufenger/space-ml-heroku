/* data route*/ 
var urlStars = "/api/v1.0/stars";

// d3.json(urlStars).then(function (response) {

    // var lastInput = 
    // console.log(response);

    // for (var i = 0; i < response.length; i++) {

    // var lastInput = response[-1][0];
    // console.log(response[-1]);
// });

console.log("hello");

document.getElementById("time").textContent = "" + new Date();